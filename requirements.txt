# ── LiveKit / agents stack ─────────────────────────────────────────────────
livekit-agents>=1.0.0
livekit-api>=0.8.0
livekit-plugins-google==1.5.11
livekit-plugins-deepgram>=0.8.0
livekit-plugins-silero>=0.8.0
livekit-plugins-noise-cancellation

# ── Gemini Live realtime types (used by agent.py for SessionResumptionConfig etc.) ──
google-genai>=0.3.0

# ── Web server ─────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── Database ───────────────────────────────────────────────────────────────
supabase>=2.0.0

# ── HTTP / async ───────────────────────────────────────────────────────────
httpx>=0.25.0
aiohttp>=3.9.0
certifi>=2024.0.0

# ── Misc ───────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
apscheduler>=3.10.0
twilio>=8.0.0
# google-api-python-client / oauthlib left out: not used (we use livekit-plugins-google
# + google-genai directly). Add them back only if you wire in Google Calendar/etc.
